# Copy to .env and fill in values. Never commit .env.

# Backend the Vite dev proxy forwards /api and /health requests to.
# - Running in container: http://host.containers.internal:8080
# - Running locally (no container): http://localhost:8080
VITE_API_TARGET=http://host.containers.internal:8080
